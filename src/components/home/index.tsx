import React from 'react';
import * as Styled from './index.styled';

const HomeComponent: React.FC = () => {
	return (
		<Styled.Container>
			<Styled.LeftSubContainer></Styled.LeftSubContainer>
			<Styled.RightSubContainer></Styled.RightSubContainer>
		</Styled.Container>
	);
};

export default HomeComponent;
